<p id="notice"><%= notice %></p>

<h1 class="pageheader"><%= @artwork.title %></h1>
<br>
<p class="pagetext"><%= image_tag(@artwork.art_url.to_s, class: "imagebox") %></p>
<p class="pagetext"><%= @artwork.description %></p>
<p class="pagetext">Created on: <%= @artwork.created_on.strftime("%B-%d-%Y") %></p>
<p class="pagetext">Artist: <%= link_to @artwork.user.vname, user_path(@artwork.user) %></p>
<br>
<p class="pagetext"><%= link_to 'Back', mainfolder_subfolder_path(@subfolder.mainfolder, @artwork.subfolder) %></p>
