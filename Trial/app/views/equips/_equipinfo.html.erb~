<% @equips.each_with_index do |equip, index| %>
   <%# if pet.reviewed? && !pet.monster? %>
      <div class="inner">
         <table>
            <tr>
               <td><%= link_to equip.inventory.item.name, petowner_equip_path(@petowner, equip) %></td>
               <!--Prevents a visting user from seeing the radio button of a pet if not logged in -->
               <% if !current_user.nil? %> <!---Difference when current_user is nil-->
                  <td><%#= radio_button_tag :pet_id, pet.id %></td>
               <% end %>
            </tr>
            <tr>
               <td></td>
               <td>HP: <%= equip.inventory.item.hp %></td>
            </tr>
            <tr>
               <td></td>
               <td>ATK: <%= equip.inventory.item.atk %></td>
            </tr>
            <tr>
               <td></td>
               <td>DEF: <%= equip.inventory.item.def %></td>
            </tr>
            <tr>
               <td></td>
               <td>SPD: <%= equip.inventory.item.spd %></td>
            </tr>
         </table>
      </div>
      <!--Sets how many pets to display per line-->
      <% if ((index + 1)%3) == 0 %>         
         <div class="outer"></div>
      <% end %>
   <%# end %>
<% end %>
