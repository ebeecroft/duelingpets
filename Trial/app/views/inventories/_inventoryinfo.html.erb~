<% @inventories.each_with_index do |inventory, index| %>
   <%# if pet.reviewed? && !pet.monster? %>
      <div class="inner">
         <table>
            <tr>
               <td><%= link_to inventory.item.name, user_inventory_path(@user, inventory) %></td>
               <!--Prevents a visting user from seeing the radio button of a pet if not logged in -->
               <% if !current_user.nil? %> <!---Difference when current_user is nil-->
                  <td><%#= radio_button_tag :pet_id, pet.id %></td>
               <% end %>
            </tr>
            <tr>
               <td></td>
               <td>HP: <%= inventory.item.hp %></td>
            </tr>
            <tr>
               <td></td>
               <td>ATK: <%= inventory.item.atk %></td>
            </tr>
            <tr>
               <td></td>
               <td>DEF: <%= inventory.item.def %></td>
            </tr>
            <tr>
               <td></td>
               <td>SPD: <%= inventory.item.spd %></td>
            </tr>
            <tr>
               <td><%#= button_to 'Approve', pets_reviews_path(:pet_id=>pet.id) %></td>
               <td><%= button_to "Equip me" %> <%# new_petowner_equip_path(:petowner=>petowner.id) %>Or<%= button_to "Use me", user_user_id_inventories_id_path(inventory), method: :put %></td>
            </tr>
         </table>
      </div>
      <!--Sets how many pets to display per line-->
      <% if ((index + 1)%3) == 0 %>         
         <div class="outer"></div>
      <% end %>
   <%# end %>
<% end %>
